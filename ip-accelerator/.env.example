# No API keys required for basic operation
# All data sources used are free and public

# Optional: increase USPTO rate limit with a key
# USPTO_API_KEY=

# Cache settings (override config/settings.yaml)
# CACHE_TTL_HOURS=24
